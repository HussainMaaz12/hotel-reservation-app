// Basic Vite config (CommonJS) — intentionally minimal so Vite will use defaults.
// Removed: use frontend/vite.config.mjs (ESM) for frontend dev server
// This placeholder prevents accidental usage of the root Vite config.
module.exports = {};
