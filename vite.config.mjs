import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// Removed: use frontend/vite.config.mjs for the ESM frontend Vite config.
// This placeholder prevents accidental loading of the root ESM config.
export default {};
